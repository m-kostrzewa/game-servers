---
# Handlers for Abiotic Factor server

- name: restart abiotic service
  systemd:
    name: abiotic-factor
    state: restarted
    daemon_reload: yes
  async: 600  # 10-minute timeout for SteamCMD update + Wine/server startup
  poll: 10    # Check every 10 seconds
